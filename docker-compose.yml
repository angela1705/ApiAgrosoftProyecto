version: "3.9"

services:
  frontend:
    image: wmauriciodev/agrosoft_frontend:latest
    ports:
      - "5173:80"
    stdin_open: true
    tty: true

  backend:
    image: juan66016/agrosoft_backend:latest
    ports:
      - "8000:8000"
    depends_on:
      - db
    environment:
      - POSTGRES_DB=agrosoft
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=root
      - DB_HOST=db
      - DB_PORT=5432

  db:
    image: postgres:17
    restart: always
    environment:
      POSTGRES_DB: agrosoft
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: root
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5433:5432"

  documentacion:
    image: juan66016/agrosoft_documentacion:latest
    ports:
      - "4321:4321"
    restart: unless-stopped

volumes:
  postgres_data:
